{
  "name": "LS22 Word Search Game",
  "version": "0.0.1",
  "description": "",
  "keywords": "",
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build index.html --out-dir ../../static/word-search/  --public-url ./",
    "build-dev": "parcel build index.html --out-dir ../../static/word-search/  --public-url /ls-22/word-search",
    "build-prod": "parcel build index.html --out-dir ../../static/word-search/  --public-url ./",
    "dev": "parcel index.html --open --port 8080",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-static-files-copy": "^2.6.0",
    "sass": "^1.52.3",
    "typescript": "^4.7.3"
  }
}
